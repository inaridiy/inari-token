{
  "name": "web3_template",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "eslint": "8.8.0",
    "eslint-config-next": "^12.0.10",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-import": "^2.25.4"
  },
  "scripts": {
    "dev": "yarn workspace app dev",
    "test": "yarn workspace contract hardhat test",
    "build": "yarn workspace @inaridiy/useful-web3 build",
    "watch": "yarn workspace @inaridiy/useful-web3 watch"
  },
  "workspaces": {
    "packages": [
      "projects/*"
    ]
  }
}